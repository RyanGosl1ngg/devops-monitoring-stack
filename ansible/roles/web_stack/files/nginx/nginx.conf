events {}

http {
    server {
        listen 80;

        location = /stub_status {
            stub_status;
            allow all;
            access_log off;
        }

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}
